# Changelog

## 2025-11-12

### 📦 Файлы и MinIO
- Перешли на хранение MinIO object name вместо presigned URL для всех новых загрузок (`service/files.py`, `service/presentation_converter.py`).
- В `FileUploadResponse` и связанных схемах теперь `file_id` = object name, добавлены реальные `file_size`, `content_type` и новый `minio_path`.
- `get_file_info` / `delete_file` принимают как полные MinIO пути, так и старые записи; корректно определяют бакет через `_determine_bucket_and_object`.

### 📚 Подразделы и презентации
- `_prepare_slides_response` приводит старые структуры слайдов к новому формату, генерирует свежие ссылки и обновляет запись в БД.
- `delete_old_slides` корректно удаляет и «старые» URL-представления, и новые object name (исправлены отступы).
- Исправлены отступы и логирование в `list_subsections_service`: кэш и БД возвращают одинаковый формат, все значения кэшируются.

### 🔧 Прочее
- Исправлены `IndentationError` в `service/subsections.py` и `service/presentation_converter.py`, из-за которых uvicorn не стартовал.
- Уточнены логи: видно, идет ли ответ из кэша или из БД, и удалились ли конкретные слайды/превью.

> Для проверки основной проблемы с истекающими ссылками: запросите подраздел/тему после того, как старая ссылка истекла, и убедитесь, что API возвращает новый presigned URL. Frontend для обновления URL может использовать `/api/v1/files/management/{file_id}/info`.

